#google-cloud-firestore>=2.15.0  # Ensure a recent version is installed
#Flask
#python-dateutil

#mcp[cli]>=0.5.0
#flask>=2.3
#google-cloud-firestore>=2.14
#python-dateutil>=2.9

# original deps your CF needs
flask>=2.3
google-cloud-firestore>=2.14
python-dateutil>=2.9

# pin the compatible MCP version
mcp[cli]==0.4.1
functions-framework>=3.5     # for our fake Request helper
werkzeug>=3                  # already a Flask transitive dep